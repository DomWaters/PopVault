// Fake "Collectr" import payload — stands in for the real API response.
export const collectrSample = {
  itemCount: 142,
  suggestedValue: 18400,
}

// Fallback used when someone enters their collection manually instead.
export const manualEntryDefault = {
  itemCount: 1,
  suggestedValue: 500,
}
